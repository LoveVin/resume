{
  "name": "resume",
  "scripts": {
    "dev": "parcel src/index.html --open",
    "build": "rm -rf dist && parcel build src/index.html --public-url ./"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel": "^2.12.0"
  }
}
